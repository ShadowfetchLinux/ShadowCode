{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Embedded ShadowCode interface only; all filesystem and process actions use the guarded Rust command service.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
